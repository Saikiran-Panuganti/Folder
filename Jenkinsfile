pipeline{
    agent any
    stages{
        stage("Git Checkout"){
            steps{
                echo "Repository checked out successfully"
                sh 'pwd'
                sh 'ls -lrt'
            }
        }
    }
}
